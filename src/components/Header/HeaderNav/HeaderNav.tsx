import React from "react";

const HeaderNav = (props: any) => {
    return(
        <div>
            <props.SectionContainer>
            </props.SectionContainer>
        </div>
    )
}

export default HeaderNav